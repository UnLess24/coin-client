info:
  contact: {}
paths:
  /healthcheck:
    get:
      consumes:
      - application/json
      description: do check service
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            type: string
      summary: проверка работоспособности сервиса
      tags:
      - example
swagger: "2.0"
